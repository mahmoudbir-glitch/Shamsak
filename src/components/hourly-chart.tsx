export function HourlyChart({values}:{values:number[]}){const max=Math.max(...values,1);return <div className="flex h-36 items-end gap-1 overflow-hidden rounded-xl bg-slate-50 p-3">{values.map((v,i)=><div key={i} className="flex h-full flex-1 flex-col justify-end gap-1"><div title={`${v.toFixed(1)} kW`} className="w-full rounded-t-md bg-emerald-400/80" style={{height:`${Math.max(4,v/max*100)}%`}}/><span className="text-center text-[9px] text-slate-400">{i}</span></div>)}</div>}