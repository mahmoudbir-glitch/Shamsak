import {NextResponse} from 'next/server';
export const runtime='nodejs';
export async function GET(){const url=new URL('https://api.open-meteo.com/v1/forecast');url.searchParams.set('latitude','33.8938');url.searchParams.set('longitude','35.5018');url.searchParams.set('timezone','Asia/Beirut');url.searchParams.set('forecast_days','4');url.searchParams.set('daily','weather_code,temperature_2m_max,temperature_2m_min,precipitation_probability_max,sunrise,sunset');url.searchParams.set('hourly','shortwave_radiation,cloud_cover,temperature_2m,precipitation_probability,weather_code');try{const r=await fetch(url,{next:{revalidate:1800}});if(!r.ok)return NextResponse.json({error:'weather_unavailable'},{status:502});return NextResponse.json(await r.json(),{headers:{'Cache-Control':'s-maxage=1800, stale-while-revalidate=3600'}})}catch{return NextResponse.json({error:'weather_unavailable'},{status:503})}}